@model IEnumerable<MyTube.Models.Subscriber>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.Pass)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User1.Pass)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.Pass)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User1.Pass)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubID })
        </td>
    </tr>
}

</table>
