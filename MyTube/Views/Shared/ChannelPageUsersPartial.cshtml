@model IEnumerable<MyTube.DTO.UserDTO>
@foreach (var item in Model)
{
    <div class="col-md-4">
        <a href="@Url.Action("ChannelPage/"+item.Username,"Home")" target="_self">
            <img id="videoImage" src="@item.ProfilePictureUrl" alt="video" style="width:100%">
            <div class="caption">
                <p id="titleBar">@item.Username</p>
            </div>
            <p id="followingInfo">Followers: @item.SubscribersCount Videos: @item.VideosCount</p>
        </a>
    </div>
}
